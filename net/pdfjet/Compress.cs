using System;

namespace PDFjet.NET {
    /// <summary>
    /// Enum used to specify compression.
    /// </summary>
    public enum Compress {
        /// <summary>Compresses the embedded file.</summary>
        YES,
        /// <summary>Stores the embedded file uncompressed.</summary>
        NO
    }
}
